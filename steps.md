# things you still need to do after installing NixOS

set firefox default search engine to duckduckgo (this might be possible in NixOS configuration, see programs.firefox.policies or programs.firefox.preferences)
set static ip for home wifi

run these commands to set up git
```sh
git config --global user.email "casperhowse@gmail.com"
git config --global user.name "Casper Howse"
```

